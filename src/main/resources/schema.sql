CREATE TABLE pet (
                         id INT AUTO_INCREMENT PRIMARY KEY,
                         pet_name VARCHAR(30),
                         pet_kind VARCHAR(30),
                         pet_sex INTEGER,
                         pet_vax_yes BOOLEAN,
                         pet_vax_no BOOLEAN
);